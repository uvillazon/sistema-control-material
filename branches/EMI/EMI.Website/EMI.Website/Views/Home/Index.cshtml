@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutExtjs.cshtml";
}

<script type="text/javascript">
    //Ext.onReady(function() {
    //    Ext.Msg.alert("asdasdad");
    //});
   Ext.Loader.setConfig({ enabled: true });
   Ext.Loader.setPath('App', 'App');
   Ext.Loader.setPath("Extensible","App/extensible");
    Ext.require([
    'App.*'
    ]);
    
   Ext.application({
        name: 'App',
        appFolder: 'App',
        //controllers: ['App.Controller.Main'],

        launch: function () {
            Ext.tip.QuickTipManager.init();
            var panel = Ext.create('App.View.Principal.Principal', {
                //            renderTo: 'contenido'
            });
        }
    });
   </script>
